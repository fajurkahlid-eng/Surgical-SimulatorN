# Copy to .env and adjust. Never commit real .env to git.

PORT=3001

MYSQL_HOST=localhost
MYSQL_USER=root
MYSQL_PASSWORD=
MYSQL_DATABASE=surgical_training

# Required for production: long random string (openssl rand -base64 48)
JWT_SECRET=
JWT_EXPIRES_IN=7d
