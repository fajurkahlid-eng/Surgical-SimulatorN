Place these GLB files in this folder (exact names):

  mainbuild.glb   — hospital / building environment
  doctor.glb       — doctor (shown parented to first-person camera)
  BodyHuman.glb   — patient (positioned via WORLD_CONFIG in src/world.js)

After the first load, adjust positions and scales in src/world.js (WORLD_CONFIG) if the scene is misaligned.
